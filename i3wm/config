# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
#Set the border to normal for new windows
new_window normal
#for_window [class="^.*"] border pixel 1
#workspace_layout tabbed
focus_follows_mouse no

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:Droid Sans Mono 12
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.
#border changing
bindsym $mod+z border toggle
#scratchpad
bindsym $mod+m move scratchpad
bindsym $mod+o scratchpad show, resize shrink height 10000,resize grow height 600 ,resize shrink width 10000,resize grow width 1200, move window to position 50 50
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
#quickswitch
set $vbox_mod  "vboxes: n for normal, h for headless, l for sdl GUI"
mode $vbox_mod {
bindsym h exec ~/.bin/vmrun_headless; mode "default"
bindsym n exec ~/.bin/vmrun_default; mode "default"
bindsym l exec ~/.bin/vmrun_sdl; mode "default"
# back to normal: Enter or Escape
bindsym Return mode "default"
bindsym Escape mode "default"
}
bindsym $mod+b mode $vbox_mod
bindsym $mod+c exec xtrlock
bindsym Mod1+space exec /usr/bin/quickswitch.py -d 'dmenu -i -l 50 -fn "Droid Sans Mono 12"'
#bindsym $mod+o exec /usr/bin/quickswitch.py -w  -d 'dmenu -i -l 50 -fn " Droid Sans Mono 12"'
bindsym $mod+BackSpace exec /usr/bin/quickswitch.py -w  -d 'dmenu -i -l 50 -fn "Droid Sans Mono 12"'
# mocp controls with multimedia keys
bindsym XF86AudioPlay exec mocp -G #play/pause toggle
bindsym XF86AudioStop exec mocp -s
bindsym XF86AudioPrev exec mocp -r
bindsym XF86AudioNext exec mocp -f
# start a terminal
bindsym $mod+Return exec urxvt -e tmux attach -t gen:3
bindsym $mod+Shift+Return exec urxvt 
#bindsym $mod+Shift+Return exec urxvt -e tmux new -s temp || tmux attach -t temp
#start conkeror
#bindsym $mod+m exec qutebrowser
bindsym $mod+Shift+n exec vimprobable2
bindsym $mod+n exec conkeror
#bindsym $mod+n exec conkeror
#run netbeans
#bindsym $mod+b exec /home/komm/.bin/netbeans-8.1/bin/netbeans 

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+p exec dmenu_run -fn "Droid Sans Mono 14"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change container focus
#bindsym $mod+Tab focus parent; focus right; focus child
#bindsym $mod+Shift+Tab focus parent; focus left; focus child
# change focus
bindsym $mod+l focus child; focus right
bindsym $mod+h focus child; focus left
bindsym $mod+k focus child; focus up
bindsym $mod+j focus child; focus down

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+y split h

# split in vertical orientation
bindsym $mod+u split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen
bindsym Pause fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle
bindsym $mod+space floating toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
#Next/Previous Workspace
bindsym Mod1+Tab focus right
bindsym Mod1+Shift+Tab focus left
bindsym $mod+Tab workspace back_and_forth

# switch to workspace
bindsym $mod+1 workspace 1:@
bindsym $mod+2 workspace 2:*
bindsym $mod+3 workspace 3:>_
bindsym $mod+4 workspace 4:V
bindsym $mod+5 workspace 5:VP
bindsym $mod+6 workspace 6:PD
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:@
bindsym $mod+Shift+2 move container to workspace 2:*
bindsym $mod+Shift+3 move container to workspace 3:>_
bindsym $mod+Shift+4 move container to workspace 4:V
bindsym $mod+Shift+5 move container to workspace 5:VP
bindsym $mod+Shift+6 move container to workspace 6:PD
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10
#lightdm switch to greeter
#bindsym $mod+Shift+v exec dm-tool switch-to-greeter
bindsym $mod+Shift+v exec "T -e tmux attach -t vim"
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
# volume controls
#bindsym $mod+z exec ~/amixerplus
#bindsym $mod+x exec ~/amixerminus
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym Mod1+KP_Add exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume NOT WORKING
#ERRORE: pactl: opzione non valida -- "5"
bindsym XF86AudioLowerVolume exec --no-startup-id amixer sset 'Master' 5%- #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

#screen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

bindsym $mod+Shift+z exec "xbacklight -dec 1"
bindsym $mod+Shift+x exec "xbacklight -inc 1"
# resize window (you can also use the mouse for that)
#bindsym $mod+Shift+o mode "launcher"
set $w_launch "[g] Whonix Gateway - [w] Whonix Workstation"
#bindsym $mod+Shift+o mode $w_launch
#mode $w_launch{
##
#        bindsym g exec   vboxsdl --startvm 'Wonix-Gateway';mode "default"
#        bindsym w exec   vboxsdl --startvm 'Whonix-Workstation';mode "default"
#        bindsym Escape mode "default"
#        bindsym Return mode "default"
#}
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"
#Client colors#
#
# class                 border  backgr. text    indicator child_border
#client.focused          #4C7899 #000000 #D23434 #2E9EF4   #285577
#client.focused_inactive #4C7899 #000000 #D23434 #2E9EF4   #285577
#client.unfocused #4C7899 #000000 #7E1F1F #2E9EF4   #285577
#
#Applications to workspace
assign [class="Vimprobable2"]→5:VP
assign [class="Iceweasel"]→1:@
assign [class="Firefox"]→1:@
assign [class="Conkeror"]→1:@
assign [title="tmux"]→3:>_
for_window [title="tmux" ]  focus
assign [class="VirtualBox"]→4:V
assign [class="VBoxSDL"]→4:V
assign [class="Tor Browser"]→4:V
assign [class="TorLauncher"]→4:V
assign [class="mplayer2"]→2:*
assign [class="mpv"]→2:*
assign [class="Vlc"]→2:*
assign [class="PdWindow"]→6:PD
assign [class="x2504ca0"]→6:PD
assign [class="Kodi"]→2:*

# startup
exec ~/.i3/startup
exec ~/.i3/scratchterm
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    mode dock
    position top
    workspace_buttons yes
    #font pango:DejaVu Sans Mono 12
    font pango:DroidSans 11, Awesome 8
    strip_workspace_numbers yes # issue with a leading whitespace

    colors {
            background #253941
            statusline #9e958e

			focused_workspace  #08202a #08202a #e7dfd9
			inactive_workspace #253941 #253941 #9e958e
			urgent_workspace   #1b1d1f #c6a78a #000000
            #focused_workspace #D23434  #dedede
            active_workspace #FFFFFF  #000000
            #inactive_workspace #D23434   #999999
            #urgent_workspace #2e3436 #9a878f
        }
#        status_command i3status
#         status_command /home/komm/.i3/conkyTest
#         status_command py3status
          status_command i3blocks -c ~/i3blocks.conf
}
#client.focused #000000 #dbdbdb #363636 #859900
#client.unfocused #FFFFFF #999999 #dbdbdb #859900
#:::::::::::::::: MARCO DE VENTANA ::::::::::::::::::#
################################################################
# class           		 border  backgr  text   indicator child_border 
client.focused		 	#FFFFFF #1d3037 #ffffff  #ffffff   #1d3037
client.focused_inactive 	#00727f #1d2121 #a1a1a1  #484e50   #1d2121
client.unfocused 		#00727f #272727 #fdfdfd  #bfbfbf   #272727	
client.urgent   		#4a4b4c #4a4b4c #fdfdfd  #fbfbfb   #4a4c4b
#client.placeholder     		#000000 #0c0c0c #ffffff  #000000   #0c0c0c

#client.background       	#111111
################################################################
